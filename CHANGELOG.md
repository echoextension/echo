# 更新日志 / Changelog

## 1.4.0 - 待发布（候选基线：2026-09-01）

> 1.4.0 尚未提交扩展商店。本条目合并记录本版本原计划的新功能、候选期缺陷修复、核心代码重构及自动化工程治理。正式发布日期将在商店发布前确认。
>
> 本轮重构的产品目标是：除下方明确列出的修复和安全收紧外，保持既有界面、设置、存储数据与主要交互不变。自动化门禁通过不替代真实 Edge、真实 B站/知乎和视觉质量的长期人工测试。

### 新增功能与产品调整

- 新增知乎黑名单内容过滤：
  - 用户明确授权后读取知乎官方黑名单，在已支持且能够可靠识别作者的回答、信息流和评论中折叠命中内容；
  - 提供临时查看入口；无法确认作者身份时保持放行，不隐藏未知内容；
  - 黑名单快照、账号标识和过滤开关仅保存在当前设备，不进入云同步或备份；
  - 同步只有完整成功后才替换旧快照；失败或取消继续保留旧数据。
- 新增 B站推荐回退：
  - 在首页原生「换一换」下方提供上一批、下一批导航，不修改原生按钮；
  - 每个标签页会话最多保留最近 10 批推荐；
  - 结构化保存标题、封面、目标链接、作者、日期、时长、播放数、弹幕数及普通视频/广告等展示类型；
  - 刷新首页或同标签进入视频后返回时恢复当前批次和索引；关闭标签页或关闭功能后清理对应会话；
  - 不同 B站标签页的数据相互隔离，无法可靠恢复的字段不伪造。
- 更新首次运行导览、设置页和官网，增加 B站与知乎专属增强的功能介绍和效果演示。
- 优化设置页滚动导航、功能说明和 FRE 页面交互细节。
- 优化高 DPI 小屏设备适配：设置页增加低高度紧凑布局并消除侧栏内部滚动，FRE 网站增强页移除会导致演示区变形的缩放补偿。
- 新标签页热搜榜对全新安装改为默认关闭，升级用户保持原设置；移除数据量不足的电影榜和剧集榜。
- 超级拖拽恢复为默认开启。
- 下架“网页关联搜索推荐”实验室功能，删除对应生产代码、消息路由和模型接口。

### 标签页、窗口与 Service Worker 修复

- 修复关闭标签后遗留的上一活动标签状态可能污染后续判断，导致关闭中间标签时跳转到错误标签的问题。
- 修复不同浏览器窗口共用关闭处理锁，导致窗口 A 的关闭事务干扰窗口 B 标签焦点的问题；关闭状态现按 `windowId` 隔离。
- 修复标签插入基点缓存仍指向旧标签时，新标签没有出现在当前活动标签右侧的问题；创建前使用本次真实活动标签校正基点，并保留连续创建顺序。
- 修复扩展创建标签时，真实 Chromium 的 `onCreated` 事件可能早于 `tabs.create()` Promise 返回，从而使同一标签被重复判断或移动的竞态。
- 修复多个后台标签快速连续创建时可能乱序的问题，引入按窗口串行的创建队列和明确的扩展自有创建标记。
- 修复关闭活动标签、关闭非活动标签、关闭最左/最右标签及跨窗口交错操作中的焦点边界。
- 修复老板键最小化浏览器后，Manifest V3 Service Worker 被回收时丢失恢复状态的问题；窗口快照现在存入 session，并可在 Worker 重启后继续恢复。
- 知乎同步任务增加 session 元数据；Worker 重启时关闭遗留同步窗口、清理任务键，并向设置页报告可解释的中断失败，不接管半完成任务。

### 消息、权限与后台安全收紧

- 建立统一的消息 action 与 Port 注册表，后台只有一个 runtime 消息入口；未知 action 和无效参数不再静默接受。
- 新标签导航只允许 HTTP(S) 或本扩展页面；代理请求只允许既定的百度、头条 HTTPS GET 数据端点。
- B站推荐历史消息限制为 schema v3、最多 10 批，并校验消息必须来自 `www.bilibili.com` 对应标签页。
- FRE 的 F2/F3 演示使用显式 `demo` 来源；后台仅允许本扩展四个 FRE 页面使用该来源，普通网页不能伪造 demo 绕过用户设置。
- 图片防盗链请求使用精确 URL 的临时 DNR Referer 规则，不再扩大到整个图片域名。
- 修复并发图片请求共用固定 DNR 规则 ID、发生覆盖或提前清理的问题；规则 ID 现在串行分配且请求结束后单独删除。

### NTP 启动、搜索与壁纸修复

- 修复壁纸数据或网络请求未完成时，NTP 搜索框、设置按钮和壁纸开关迟迟没有绑定的问题；基础交互不再等待壁纸网络。
- 修复搜索建议请求乱序：旧关键词的慢响应不再覆盖较新关键词的建议。
- 修复壁纸接口返回 404、HTML、空响应或错误 Content-Type 时仍写入 IndexedDB 的问题。
- 修复 IndexedDB 中的图片 Blob 已损坏或无法解码时停止在坏缓存的问题；现在删除坏缓存并尝试网络回源。
- 修复旧壁纸请求晚于新请求完成时，覆盖新壁纸标题、收藏状态和信息卡的问题；渲染请求增加序号并拒绝过期提交。
- 修复图片尚未成功解码时就提前提交“当前壁纸”和 UI 状态，导致旧背景配新标题的问题；现在只在图片成功呈现后提交状态。
- 修复连续图片损坏时遍历整份壁纸历史、产生大量请求和日志的问题；每次显示最多执行初始请求加三次回退。
- 修复壁纸加载流程将 `window.fetch` 作为对象方法调用，真实 Chromium 抛出 `Illegal invocation` 并导致壁纸完全无法加载的问题。
- 修复页面编排将 `window.setTimeout` 作为对象方法调用，真实 Chromium 抛出 `Illegal invocation` 并中断 NTP 初始化的问题。
- 修复从本地或收藏壁纸的单张锁定状态切换到“必应每日壁纸”时，来源状态和实际壁纸不一致的问题；明确选择每日来源会解除单张锁定，但保留收藏和本地图片。
- 修复壁纸设置保存失败后，页面已经显示关闭、锁定、极简或空白模式，但刷新后恢复旧状态的问题；相关操作现在会回滚内存、开关和页面 class。
- 修复收藏模式最后一张可用壁纸被删除后仍停留在空收藏模式的问题；未锁定时自动回退必应每日并持久化。

### 自定义壁纸与收藏同步修复

- 自定义壁纸上传改为原子流程：显示图和缩略图任一写入失败时同时回滚，不提交孤立收藏或锁定状态。
- 修复收藏或设置持久化失败后，IndexedDB 已留下 Blob、页面内存却无法在重启后恢复的问题；上传失败会恢复历史、收藏、锁定和预览状态，并清理两份 Blob。
- 串行处理自定义壁纸上传，修复并发上传同时通过 10 张上限检查或同一毫秒生成重复 ID 的问题。
- 自定义壁纸 key 增加实例内单调保证，避免历史记录或 Blob 被同名覆盖。
- 删除自定义壁纸改为可回滚事务：收藏、锁定或 Blob 删除任一步失败时，不留下“元数据已删但图片仍在”或相反状态。
- 修复删除壁纸时后台懒压缩尚未结束，压缩任务随后把已删除 Blob 重新写回的问题。
- 修复从收藏面板删除当前自定义壁纸后，页面仍展示已经从历史和 IndexedDB 删除的图片；成功删除后只重新选择并显示一次可用壁纸。
- 修复其他设备同步来的 `custom:` 收藏在本机没有 Blob 时，仍参与轮播、收藏数量和 10 张本地上限的问题；原始同步收藏继续保留，但本机选择与计数只使用实际可用项。
- 收藏 sync 与 local 降级记录增加版本化更新时间；启动和运行时均比较新鲜度，避免旧的本地降级记录覆盖更新的远端收藏，或旧远端事件覆盖本机尚待同步的操作。
- sync 配额恢复后会重试上传本地降级收藏，并在成功后删除降级记录；继续兼容旧数组格式。

### 搜索框、输入增强与设置行为修复

- 修复悬浮搜索框关闭后动画帧、频谱动画或热词轮询仍持续运行的问题，隐藏时完整停止，重新显示时按需恢复。
- 普通网页、NTP、设置页和文档页统一服从鼠标手势、精细缩放、F2/F3 与超级拖拽用户开关。
- FRE 保留独立演示能力：即使用户尚未完成设置或已关闭全部输入增强开关，四个 FRE 页面仍可演示鼠标手势、精细缩放、F2/F3 和超级拖拽；demo 不读取、覆盖或写回用户设置。
- 修复右键按下后用户即时关闭鼠标手势时，不发送结束消息而可能让后台保持“右键仍按下”状态的问题。
- 统一可编辑区域判断、拖拽距离、URL 识别、协议补全、拖拽意图、缩放步进及 25%～500% 边界，减少不同页面行为漂移。
- 设置页删除重复的输入增强实现，改为加载同一共享策略和页面适配层。

### 备份、设置迁移与数据一致性修复

- 备份导入改为“全部字段先校验、全部通过后写入”，修复损坏备份已经写入部分设置和收藏的半恢复状态。
- 备份格式增加 schema 版本、设置白名单、类型与枚举校验；未知新字段安全忽略，已知字段类型错误明确拒绝。
- 自定义壁纸 Blob 不进入备份；导入时过滤 `custom:` 收藏，并清除无法在目标设备恢复的自定义壁纸锁定。
- sync 与 local 跨区域写入失败时，只回滚本次触及的键，不清空其他并发写入的用户设置。
- 建立唯一设置 schema，统一默认值、存储区域、类型、枚举、对象校验和废弃状态。
- 升级时迁移旧 `activateLeftTab`，清洗非法设置，并删除已无生产实现的旧书签栏、搜索引擎和侧栏相关键。
- 补齐过去未纳入后台安装默认值的精细缩放大步进和搜索框跟随缩放设置。

### 核心代码重构

- 后台 Service Worker 从大型单体拆为薄装配入口和独立服务：设置、命令、标签协调、消息路由、图片、网络、B站会话和知乎同步分别拥有明确状态与事件监听器。
- NTP 从大型单体拆为薄装配入口和 24 个领域模块，分别管理壁纸状态、数据源、缓存、渲染、收藏命令、自定义壁纸、设置面板、收藏面板、信息卡、主题、通知、空白模式、热搜、搜索、缩放和启动编排。
- 设置、消息和输入算法形成单一权威来源，页面文件只保留必要的环境适配；没有引入新的前端框架，也没有同时进行大面积 CSS 或视觉重做。
- 保持原有主要存储 key、收藏数据、设置数据和 IndexedDB 数据边界，并补充旧对象、旧数组、损坏值和缺失值迁移测试。
- 清理已确认没有入口或调用方的旧消息分支、重复路由、旧书签栏实现和关联搜索遗留代码。

### 自动化测试与发布工程

- 引入 Vitest、jsdom、fake IndexedDB 和有状态 Chrome API fake，覆盖设置、消息、标签事件、存储、Port、DNR、页面 DOM、B站/知乎脱敏 fixture 及 NTP 领域状态机。
- 修复经典脚本通过 `eval`/VM 执行时无法映射 V8 覆盖率、错误显示 0% 的测试基础设施问题。
- 当前自动化基线包含 33 个测试文件、167 项测试；覆盖率实测为：语句 64.98%、分支 54.52%、函数 65.37%、行 68.56%。
- 覆盖率门禁分别设为：语句 64%、分支 53%、函数 64%、行 67%；后续下降会使统一检查失败。
- 增加 8 项独立 profile、零重试的 Chromium 扩展集成测试，覆盖：
  - Manifest V3 Service Worker 和扩展页面加载；
  - 本地 HTTP 页面内容脚本注入与消息通信；
  - F2/F3、相邻标签创建、移动和关闭激活；
  - Worker 主动停止后的按需恢复；
  - DNR Referer 规则实际命中与清理；
  - NTP 壁纸写入 IndexedDB 后的断网恢复；
  - 旧设置在真实扩展环境中的迁移；
  - 用户关闭输入开关后 FRE demo 仍然可用。
- 建立静态发布门禁：Manifest、package、CHANGELOG、设置页和官网版本必须一致；四类权限与审核基线发生 diff 时失败。
- 建立显式发布 allowlist、确定性 ZIP、SHA-256、严格文件清单和解压后 Chromium 加载验证；测试、探针、开发脚本和报告不会进入扩展包。
- 建立 GitHub Actions 分层流水线：PR 快速门禁、`main` Chromium 集成、手动发布候选，以及不阻塞普通 PR 的第三方 API/selector 定时监控。
- 自动化只生成并验证发布候选，不执行商店上传，也不替代真实 Edge 人工 QA。

### 1.4.0 候选版本的兼容预期

- 除本节明确修复的问题外，用户看到的设置项、NTP 主要界面、快捷键、鼠标手势、超级拖拽、悬浮搜索框及 B站视频工具应与原有行为保持一致。
- 原有设置、收藏和本地自定义壁纸应继续可读；升级不要求用户重新配置。
- 如果某项常规行为发生变化，但无法对应到上述明确修复或安全收紧，应按重构回归处理，而不是默认接受。

### 长期密集测试重点与当前边界

- 真实 Edge：重点验证 Ctrl+Q、Alt+M、F2/F3、Ctrl+滚轮、右键+滚轮、跨标签右键状态、多窗口焦点、连续快速建页/关页及浏览器最小化恢复。
- NTP：重点验证冷启动、缓存启动、完全断网、连续换壁纸、收藏/锁定/每日模式切换、空白/极简模式、自定义图片连续上传删除、跨设备收藏和存储配额失败恢复。
- B站推荐回退：重点验证长时间连续换批、10 批淘汰、刷新/前进后退、多标签隔离、广告/番剧/非普通卡片、统计字段完整性及灰度布局。
- B站视频工具：继续验证 DRM、Canvas/CSS 回退、旋转与颜色组合、全屏、页面缩放、GPU 和不同播放器结构。
- 知乎黑名单：当前狗粮记录中的“页面已打开仍误报同步通道断开”和“过滤信息流后可能中断无限滚动”仍是必须优先复验并清零的真实站点阻断项；不得因通用自动化通过而视为已解决。
- 视觉与系统环境：重点验证高 DPI、小屏、深浅主题、不同缩放比例、真实下载目录和防盗链图片。
- 商店安装、旧版本升级和签名包仍需真实 Edge 人工验收；完成前，1.4.0 只能称为候选版本，不能称为已正式发布。

## 数据维护 - 2026-08-18

- 更新 Bing 壁纸历史数据至 2026-08-18（新增 48 张壁纸）。该更新通过官网远程 JSON 生效，不单独提升扩展商店版本；扩展内置兜底数据将在下次版本发布时同步。

## 数据维护 - 2026-07-01

- 更新 Bing 壁纸历史数据至 2026-07-01（新增 36 张壁纸）。该更新通过官网远程 JSON 生效，不单独提升扩展商店版本；扩展内置兜底数据将在下次版本发布时同步。

## 数据维护 - 2026-05-26

- 更新 Bing 壁纸历史数据至 2026-05-26（新增 10 张壁纸）。该更新通过官网远程 JSON 生效，不单独提升扩展商店版本；扩展内置兜底数据将在下次版本发布时同步。

## 1.3.3 - 2026-05-09

- 壁纸数据架构重构：从打包在扩展内的 JS 全局变量改为 JSON 文件，支持从 Cloudflare 远程拉取更新（24h 缓存），本地 JSON 作为离线兜底，无需重新上架即可更新壁纸数据
- 更新 Bing 壁纸历史数据至 2026-05-09（新增 15 张壁纸）

## 1.3.2 - 2026-04-24

- 修复 B 站视频工具首次进入视频页、初次打开旋转面板时「▲ 75% ▼」偏移控件未置灰的 bug：根因为 stepper 创建时缺少初始 `.disabled` class，且 UI 构建后未立即调用 `updateBtnStates()`；此前在未旋转/未切换填充模式的场景下控件以全亮态显示，且仍可被点击污染 `fillOffset` 内部状态，现已与相邻「适应」按钮的初始化模式对齐
- 更新 Bing 壁纸历史数据至 2026-04-24（新增 19 张壁纸）

## 1.3.1 - 2026-04-18

- 修复 B 站视频工具旋转 270° + 填充模式下 ▲/▼ 偏移方向与 90° 相反的 bug：此前 270° 时 ▲ 拉到 100% 会显示脚部而非头部。统一为 ▲ 对应正立画面上半部分，与旋转角度无关，90°/270° 行为一致

## 1.3.0 - 2026-04-17

- B站视频工具「颜色反转」按钮升级为五态循环：关 → 全反转 → R 反转 → G 反转 → B 反转 → 关，单通道反转通过 SVG feColorMatrix 实现，可与通道交换任意组合（矩阵合并到同一滤镜节点），按钮文字随状态动态切换
- B站视频工具「填充」模式新增画面上下偏移控制：旋转 90°/270° 且填充模式下，在旋转面板可通过 ▲/▼ 按钮以 25% 步进调节可见区域（0%–100%，默认 75% 偏上），解决歪脖子视频填充后只能看到中间腰部区域的问题；偏移同时作用于 canvas 主渲染路径和 DRM 回退的 CSS 路径
- 修复旋转模式下颜色反转/通道交换失效的 bug：canvas 逐帧绘制路径未被 CSS filter 选择器覆盖，导致旋转开启后颜色功能叠加无效，现在两者可任意组合生效
- 修复 B站视频工具胶囊位置跨设备同步 bug：旧版本保存的是绝对像素值，不同设备视口高度不同时可能导致胶囊被挤出可视区域；改为保存相对视口高度的比例（topRatio），加载时按当前设备视口重新计算并 clamp 到合法范围，旧数据自动迁移，同时新增窗口 resize 自动重新定位
- 旋转面板按钮宽度和展开面板位置打磨：所有工具按钮统一最小宽度 92px 避免文字变化导致面板宽度跳变；面板展开时自动检测视口边界，胶囊拖拽至下方时面板自动上推保证完整可见
- 本版本由 Claude Opus 4.7 于其发布首日（2026-04-17）完成全部代码实现与细节打磨，是 Opus 4.7 在本项目的首次亮相

## 1.2.1 - 2026-04-13

- 修复 B站视频旋转功能在部分设备上 90°/270° 旋转时画面黑屏的问题：根因为 Chrome 在某些 GPU/驱动组合下对 video 元素的 CSS transform rotate 渲染异常，改用 canvas 逐帧绘制方案彻底绕过该限制，同时保留 180°/镜像的 CSS 方案不受影响；若视频受 DRM 保护无法 canvas 抓帧则自动回退 CSS 方案

## 1.2.0 - 2026-04-10

- B站视频优化工具从悬浮搜索框中独立为专属模块：在 bilibili.com 视频页左侧常驻悬浮胶囊工具条，无需 Ctrl+B 唤起即可直接使用；支持颜色反转、通道交换、旋转镜像（适应/填充两种模式）、超级变速（0.25×–3.0×），右键拖拽手柄可快速打开设置或临时隐藏
- 超级拖拽在该版本曾改为默认关闭；后续确认可与 Edge 自带拖拽同时使用、互不冲突，并已恢复默认开启
- 首次运行引导更新：第二步替换为 B站视频工具的功能演示，帮助新用户直观了解旋转适配的核心用法

## 1.1.1 - 2026-04-05

- 修复倍速功能的代码质量问题：消除监听器泄漏和全局副作用，倍速控制融入现有工具条架构
- 倍速状态纳入右上角指示器和一键重置，关闭搜索框后仍可见当前倍速并点击恢复
- 工具条 B站助手图标升级为眨眼动画小电视，tooltip 补充倍速功能说明
- FRE 引导页 B站工具条展示区重构，新增颜色反转/旋转镜像/超级变速动画 icon

## 1.1.0 - 2026-04-05

- B站视频优化工具新增极速播放（3×）和超慢速播放（0.25×）倍速按钮，突破 B站官方 0.5×-2× 限制，可在工具条直接切换；倍速按钮支持激活状态高亮，状态与 B站原生倍速控件实时同步；切换视频时自动重置为正常速度
- FRE 引导页新增 B站视频优化工具条展示，支持眼睛动画的小电视图标，两行布局清晰呈现功能与呼出方式
- 更新设置页「B站视频优化工具」功能描述，补充倍速功能说明（0.25× / 3×）
- 更新 Bing 壁纸历史数据至 2026-04-05（新增 26 张壁纸）
- 本版本由基于 OpenClaw 框架的 AI 多 Agent 团队全程自动化交付——ECHO-PM Agent 负责需求拆解与跨 Agent 调度，Dev Agent 负责代码实现，QA Agent 负责质量审查，各角色通过 session 间消息通信协作，人工仅在关键节点确认方向。用户于 06:49 以语音指令发起需求，我（ECHO-PM Agent）拆解任务、调度团队，约 3 分钟后 commit 落地，功能完整可用，仅需后续轻微细节打磨。

## 1.0.8 - 2026-03-30

- 新增 B站视频优化工具：在 bilibili 视频播放页按 Ctrl+B 呼出搜索框时，上方显示视频工具条，支持全部颜色反转、红↔绿/绿↔蓝/蓝↔红通道交换、旋转、镜像和适应/填充模式，各功能可自由组合，用于还原经过特殊处理的视频画面
- 设置页新增对应的「B站视频优化工具」开关（默认开启），位于悬浮搜索框设置项下
- 修复 Ctrl+B 搜索框在 B站等 SPA 网站上导致页面某些链接点击失效的偶现 bug：重构为 iframe 沙盒架构，彻底隔离焦点，Ctrl+B 呼出后可直接输入，不再影响页面跳转，连带优化焦点处理逻辑
- 修复上述重构中引入的搜索框反向缩放补偿在页面放大时底边距离偏移的问题

## 1.0.7 - 2026-03-19

- 更新扩展图标：基于新设计的高分辨率源图重新生成 16/48/128 三种尺寸图标（Lanczos 重采样 + 锐化），替代旧图标
- 统一图标格式为 PNG，移除 SVG 图标文件（logo.svg），所有页面改用 PNG 图标引用
- 适当放大 FRE 引导页品牌 Logo（36→44px）及文字尺寸，提升视觉辨识度
- 适当放大新标签页品牌 Logo（56→60px），设置入口图标改为与 icon16.png 原始尺寸 1:1 对应避免模糊
- 设置页新增「反馈与联系」邮箱入口，方便用户直接发送反馈（邮箱地址通过 JS 动态拼接，防止爬虫抓取）

## 1.0.6 - 2026-03-10

- 新增自定义壁纸上传：在壁纸库收藏面板中点击「+」卡片即可上传本地图片作为壁纸，上传后自动加入收藏、锁定并展示；支持 JPG/PNG/WebP 格式，最大 20MB，上限 10 张
- 自定义壁纸在收藏网格中常驻显示「本地上传」标签以区分 Bing 壁纸，信息卡片（标题/版权）自动隐藏
- 自定义壁纸仅存储在本地 IndexedDB，不参与备份导出/导入，跨设备同步时静默跳过
- 优化自定义壁纸加载速度：上传时自动压缩为 JPEG（最大 3840×2160），使 IndexedDB 存储体积与 Bing 壁纸一致，消除刷新新标签页时本地壁纸明显慢于在线壁纸的问题；已有壁纸首次显示后自动后台迁移
- 优化壁纸缓存清理：延迟执行并改用游标逐条遍历，避免 readwrite 事务阻塞壁纸显示
- 修复关闭热搜榜后打开新标签页时品牌区和搜索框明显向下跳动的问题：通过同步脚本在首帧前读取 localStorage 预设布局位置，消除异步加载导致的闪烁
- 更新 Bing 壁纸历史数据至 2026-03-10（新增 23 张壁纸，含春节、元宵节等中国特色壁纸）
- README 新增信任声明：完全开源，完全免费，无广告、无弹窗、无打扰，不碰任何隐私
- 修复隐私保护指引中 LICENSE 链接在扩展内打开会直接下载文件的问题，改为指向 GitHub 在线页面
- 修正 README 第三方服务声明：从"均非 Microsoft/Edge 官方服务"改为"均通过公开 API 访问，未使用任何内部接口"，避免对 Bing 壁纸等微软自有服务的描述不准确

## 1.0.5 - 2026-03-09

- 新增新标签页「空白页」模式：开启后隐藏搜索框、热搜、品牌区、页脚等 NTP 内容，仅保留一个弱化的设置入口，适合偏好极简空白页的用户。（本项修改感谢来自煎蛋网用户「日狗少年」提出的建议）
- 设置页左上角版本号支持点击，可在新窗口查看更新日志

## 1.0.4 - 2026-03-04

- 修复 Alt+点击快速保存图片在 Bing 图片搜索详情页（大图预览模式）无效的 bug：Bing 使用全屏 iframe（insightsOverlay）覆盖在图片上方，导致父窗口的事件监听器完全收不到点击事件。修复方案：manifest 启用 `all_frames: true` 使 content.js 注入 iframe，iframe 内仅运行 Alt+click 保存逻辑，跳过手势/拖拽等其他功能；同时增加 `elementsFromPoint` 穿透透明 DIV 覆盖层查找图片的能力
- 修复新标签页热搜榜在中等宽度屏幕（≤1400px）下内容溢出背景的布局 bug：CSS grid 和 JS 渲染均从固定 4 列改为响应式 3/4 列，并在窗口大小变化时自动重新渲染
- 优化按钮文案避免歧义：「收藏」→「收藏壁纸」/「已收藏壁纸」，「设为壁纸」→「锁定壁纸」/「已锁定壁纸」，同步更新按钮固定宽度和所有相关 Toast 提示
- 移除新标签页品牌区域的英文副标题（Edge Chinese Helper & Optimizer），简化视觉呈现
- 新增热搜榜单数据量过滤：API 返回不足 20 条数据的榜单自动隐藏且不显示翻页圆点，箭头和滚轮切换时自动跳过
- 优化悬浮搜索框（Ctrl+B）热搜交互：移除上下相邻行溢出显示，改为 hover 时容器外侧显示小箭头提示；修复 hover 后文字蓝色不恢复的 bug；hover 响应区域扩大到整个热搜容器
- 悬浮搜索框热搜推荐默认关闭（原为默认开启）
- 统一简化 NTP、设置页、FRE 四步页面的品牌区域：移除英文副标题，调整 logo 与文字尺寸比例
- 简化 manifest 插件描述文案
- 设置页实验室从「个性化」中独立为页面底部单独区块，调整卡片结构与其他设置项一致

## 1.0.3 - 2026-02-27

- 代码清理：合并 background.js 中的重复消息监听器
- 代码清理：移除 options.js 和 related-search.js 中的重复代码
- 清理搜索 URL 中的冗余参数

## 1.0.2 - 2026-02-27

- 新增「备份与恢复」功能：在设置页导出/导入新标签页壁纸收藏和全部设置，防止卸载扩展时数据丢失
- 新标签页壁纸库面板新增「备份与恢复」快捷入口
- 新增 _locales 多语言支持（中文/英文）
- 更新隐私保护指引，补充备份与恢复功能的隐私声明

## 1.0.1 - 2026-02-25

- 修正 README 中 Edge 应用商店链接（替换"即将上架"占位文字）
- 修正 GPL-3.0 许可证链接为 GitHub 绝对路径
- 尝试修复壁纸收藏跨设备同步不生效的问题（已通过本地模拟验证，待上架后真实多设备验证）

## 1.0.0 - 2026-02-22

- 首次发布
